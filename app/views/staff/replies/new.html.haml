- @title = "問い合わせへの返信"
%h1= @title

#generic-form
  = form_with model: @reply, url: confirm_staff_message_reply_path(@message) do |f|
    = render "form", f: f
    .buttons
      = f.submit "確認画面に進む"
      = link_to "キャンセル", :staff_messages

  = render "message"
