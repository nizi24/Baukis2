- @title = "プログラムの新規登録"
%h1= @title

#generic-form
  = form_with model: @program, url: :staff_programs do |f|
    = render "form", f: f
    .buttons
      = f.submit "登録"
      = link_to "キャンセル", :staff_programs
