- p = FormPresenter.new(f, self)
- p.with_options(require: true) do |q|
  = q.text_field_block(:subject, "件名", size: 40, maxlength: 80)
  = q.text_area_block(:body, "本文", rows: 6, maxlength: 800,
      style: "width: 454px")
